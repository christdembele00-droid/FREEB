# Reference configuration for FREEB backend on Koyeb.
# Configure the service in the Koyeb dashboard using backend/ as the work directory.
service:
  name: freeb-api
  type: web
  builder: docker
  workdir: backend
  dockerfile: Dockerfile
  port: 8080
  healthcheck:
    path: /
